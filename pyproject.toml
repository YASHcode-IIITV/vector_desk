[build-system]
requires = ["setuptools"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "vectordesk"
version = "1.0.0"
description = "VectorDesk: Memory-Augmented AI Office Benchmark"
requires-python = ">=3.10"